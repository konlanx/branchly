---
title: branchly
description: Give every Git branch its own database. Switch branches, and your data follows.
template: splash
banner:
  content: 🤖 This documentation is AI-generated. Spotted a mistake? <a href="https://github.com/konlanx/branchly/issues">Let us know on GitHub</a>.
hero:
  title: Give every Git branch its own database
  tagline: Switch branches, and your data follows. branchly provisions, migrates, and seeds a database per branch — automatically, on checkout.
  image:
    alt: branchly logo
    file: ../../assets/logo.svg
  actions:
    - text: Get started
      link: /branchly/start/quickstart/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/konlanx/branchly
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<CardGrid stagger>
  <Card title="Automatic on checkout" icon="rocket">
    A Git hook provisions the right database the moment you switch branches. First visit: create → migrate → seed. Every
    visit after that: instant.
  </Card>
  <Card title="Your stack, not ours" icon="puzzle">
    Plugin-based on every axis: PostgreSQL, MySQL, and SQLite datasources; Prisma, Drizzle, and Knex migrators; env file
    and direnv resolvers — and room for yours.
  </Card>
  <Card title="Fast via snapshots" icon="seti:db">
    Fully migrated-and-seeded golden images are cached by migration fingerprint, so new branches clone in well under a
    second instead of paying a full migrate + seed.
  </Card>
  <Card title="Safe by design" icon="approve-check">
    branchly only ever touches databases it created, never drops protected branches, and confirms before anything
    destructive. Your data stays yours.
  </Card>
</CardGrid>

## Two commands to a better Tuesday

```sh
npm install --save-dev branchly
npx branchly init
```

`init` detects your stack, installs the adapters it needs, writes a config, and wires up the Git hook. From then on, switching branches just works:

```sh
git checkout -b feature/login   # branchly provisions a fresh database
git checkout main               # …and instantly switches back to main's
```

Curious how it all fits together? Start with [why branchly exists](/branchly/start/why/), or jump straight to the [quickstart](/branchly/start/quickstart/).
