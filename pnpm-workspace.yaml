packages:
  - 'packages/*'

# esbuild (tsup), sharp (astro image service), and unrs-resolver
# (eslint-plugin-import-x) ship build scripts that pnpm 10 blocks by default;
# all are needed for the toolchain to work.
onlyBuiltDependencies:
  - esbuild
  - sharp
  - unrs-resolver
